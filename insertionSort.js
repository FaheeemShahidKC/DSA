function insertion(arr) {
      for (i = 0; i < arr.length; i++) {
            let curr = arr[i]
            let j = i-1
            while(j >= 0 && arr[j] > curr){
                  
            }
      }
}